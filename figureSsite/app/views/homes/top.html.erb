<h2>新着写真が
					<%= @count %>
				<span>件あります</span></h2>


<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th>タイトル</th>
      <th>投稿写真</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @figures.each do |figure|%>
      <tr>
        <td><%= link_to(figure_path(figure.id), class:"text-dark") do %>
           <strong><span> <%= figure.title %></span></strong>
           <% end %>
        </td>
        <td><%= link_to figure_path(figure.id) do %>
          <%= attachment_image_tag figure, :figure_image, :fill, 300, 300, format: 'jpeg' %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

</br>

<h6><%= link_to '投稿された写真一覧を見てみる', figures_path %></h6>