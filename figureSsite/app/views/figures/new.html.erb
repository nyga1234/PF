<h1>フィギュア画像投稿フォーム</h1>
<%= form_with model: @figure, local:true do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>説明</h4>
  <%= f.text_area :decsription %>
  <h4>画像</h4>
  <%= f.attachment_field :figure_image %>
  <%= f.submit '投稿' %>
<% end %>